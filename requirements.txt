# Core runtime dependencies — sufficient to run the ETL pipeline, hybrid
# RAG, agent orchestrator, API, and the executed fine-tuning experiment
# fully offline (default local providers require no external API keys).
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
numpy==1.26.4
rank-bm25==0.2.2
pypdf==4.3.1
beautifulsoup4==4.12.3
prometheus-client==0.20.0
python-multipart==0.0.9
httpx==0.27.2

# Testing
pytest==8.3.3
pytest-cov==5.0.0

# --- Optional extras (install as needed; NOT required for default runs) ---
# Cloud SDKs (only needed if CLOUD_PROVIDER / EMBEDDING_PROVIDER / LLM_PROVIDER
# is set to aws / azure / gcp / openai):
#   boto3                       # AWS: S3, Bedrock
#   azure-storage-blob          # Azure: Blob Storage
#   openai                      # OpenAI or Azure OpenAI chat/embeddings
#   google-cloud-storage        # GCP: Cloud Storage
#   google-cloud-aiplatform     # GCP: Vertex AI
#
# Neural local embeddings (EMBEDDING_PROVIDER=local-sentence-transformers):
#   sentence-transformers
#
# Full LoRA fine-tuning reference implementation (src/genai/finetuning/lora_finetune_demo.py):
#   torch
#   transformers
#   peft
#   datasets
#   accelerate
#
# Load testing:
#   locust
